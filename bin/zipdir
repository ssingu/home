#!/bin/bash


usage_exit()
{
  cat <<-EOF 1>&2
    Usage: ${CMDNAME} <path_to_dir>
EOF
  exit 1
}

CMDNAME=$(basename $0)
DIRNAME=$(dirname $0)

if [ 1 -ne $# ] ; then
  usage_exit
fi

PATH_TO_DIR=$1
DIR_NAME=$(basename $1)

zip -vr ${DIR_NAME}.zip ${PATH_TO_DIR} -x "*.DS_Store"
